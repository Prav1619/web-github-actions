name: Deploy static website on EC2

on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v4
    - name: connect EC2 via SSH
      run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > mykey.pem
          chmod 600 mykey.pem
    - name: Copy only HTML files
      run: |
         FILES=$(find . -type f -name "*.html")
         scp -o StrictHostKeyChecking=no -i mykey.pem $FILES \
         ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/var/www/html/
    - name: Restart Apache (httpd)
      run: |
          ssh -o StrictHostKeyChecking=no -i key.pem \
          ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} \
          "sudo service httpd restart"
          

  
    
